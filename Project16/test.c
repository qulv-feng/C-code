# include <stdio.h># include <string.h>//void test(int n)//{//	if (n < 10)//	{//		printf("%d ", n);//		return;//	}//	test(n / 10);//	printf("%d ", n % 10);//}//int main()//{//	int n = 0;//	scanf_s("%d", &n);//	test(n);//	return 0;//}//void printDigits(int n) {//    // 如果是负数，先处理负号//    if (n < 0)//    {//        printf("-");//        n = -n;//    }////    // 递归的终止条件：n 小于 10//    if (n < 10) //    {//        printf("%d ", n); // 打印最低位//        return;//    }////    // 递归调用，先打印更高位的数字//    printDigits(n / 10);////    // 打印当前最低位//    printf("%d ", n % 10);//}////int main() {//    int num;//    printf("请输入一个整数：");//    scanf_s("%d", &num);////    printf("该整数的每一位是：");//    printDigits(num);//    printf("\n");////    return 0;//}//int test(int n)//{//	if (n<=1)//	{//		return 1;//	}//	return n * test(--n);//}//int main()//{//	int n = 0;//	scanf_s("%d", &n);//	int ret = test(n);//	printf("%d ", ret);//	return 0;//}// 递归函数计算阶乘//long long factorial(int n) //{//	if (n == 0 || n == 1) {//		return 1; // 递归终止条件：0! 和 1! 都等于 1//	}//	return n * factorial(n - 1); // 递归调用//}////int main() {//	int num;//	printf("请输入一个非负整数：");//	scanf_s("%d", &num);////	if (num < 0) {//		printf("阶乘只定义在非负整数范围内！\n");//	}//	else {//		printf("%d 的阶乘是：%lld\n", num, factorial(num));//	}////	return 0;//}//int main()//{//	char arr[] = "abcdefg";//	int left = 0;//	int right = sizeof(arr) / sizeof(arr[0]) - 2;//	while (left < right)//	{//		char tem = 0;//		tem = arr[left];//		arr[left] = arr[right];//		arr[right] = tem;//		left++;//		right--;//	}//	printf("%s", arr);//	return 0;//}//void reverse(char* str)//{//	int len = strlen(str);//	char tem = 0;//	tem = *str;//	*str = *(str + len - 1);//	*(str + len - 1) = '\0';//	if (strlen(str + 1) >= 2)//	{//		reverse(*str + 1);//	}//	*(str + len - 1) = tem;//}//int main()//{//	char arr[] = "abcdefg";//	reverse(arr);//	printf("%s", arr);//	return 0;//}//unsigned Digitsum(int unsigned a)//{//	if (a < 10)//		return a;//	Digitsum(a / 10);//	return Digitsum(a / 10)+a % 10;//}//int main()//{//	int unsigned a = 0;//	scanf_s("%u", &a);//	int unsigned ret = Digitsum(a);//	printf("%d", ret);//	return 0;//}int test(int n,int k){	if (n == 1)		return 1;	if (k >= 1)	{		return n * test(n,k-1);	}	if (k < 1)		return 1;}int main(){	int n = 0;	int k = 0;	int sum = 0;	scanf_s("%d %d", &n,&k);	sum = test(n,k);	printf("%d\n", sum);	return 0;}